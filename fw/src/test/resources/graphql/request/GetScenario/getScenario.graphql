query getScenario($uuid: String!){
  getScenario(scenarioUuid: $uuid){
    scenarioType
    name
    uuid
    predicate
    action
    isDefault
    variables {
      name
      value
    }
    url
  }
}